package tachiyomi.presentation.core.theme

import androidx.compose.material3.Shapes

/**
 * The shape scale of the app.
 *
 * The values are the Material 3 Expressive scale. The scale is named here so that a component asks
 * for a role — extra small, small, medium, large, extra large — and never for a literal corner
 * size. Tune a corner in this file and every component that uses the role follows.
 */
val AppShapes: Shapes = Shapes()
